# 用户端接口对接文档

本文档为前端（小程序 / Web / APP）开发者提供用户端全部 API 接口规范。

## 目录

- [基础配置](#基础配置)
- [商品模块](#商品模块)
- [购物车模块](#购物车模块)
- [认证模块](#认证模块)
- [订单模块](#订单模块)
- [收藏模块](#收藏模块)
- [评论模块](#评论模块)
- [文章模块](#文章模块)
- [用户信息模块](#用户信息模块)
- [地址管理模块](#地址管理模块)
- [首页模块](#首页模块)
- [地区角色模块](#地区角色模块)
- [附录](#附录)

---

## 基础配置

### API Base URL

```
开发环境: http://localhost:5050/api/v1
生产环境: https://your-domain.com/api/v1
```

### 统一请求返回格式

所有接口返回统一的 JSON 格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

部分接口（尤其认证模块历史接口）会额外返回 `success` 字段（`true` / `false`）作为兼容标记，前端可忽略该字段，以 `code` 与 `message` 为准。

错误响应通常包含以下结构：

```json
{
  "code": 400,
  "msg": "技术详情或兼容字段",
  "message": "用户可读提示",
  "data": null
}
```

### 通用错误码

| 码值 | 说明 |
|------|------|
| `200` | 操作成功 |
| `400` | 参数验证失败或业务逻辑错误 |
| `401` | 未登录或 Token 失效 |
| `404` | 资源不存在 |
| `500` | 服务器异常 |

### 分页参数约定

**请求参数**（统一）:

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码，从 1 开始 |
| pageSize | Integer | 10 | 每页数量 |

**响应结构**（统一 `pagination` 对象）:

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "列表数据": [ /* ... */ ],
    "pagination": {
      "page": 1,
      "pageSize": 10,
      "total": 50,
      "pages": 5
    }
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| page | Integer | 当前页码 |
| pageSize | Integer | 每页数量 |
| total | Integer | 总记录数 |
| pages | Integer | 总页数 |

> **注意**: 除首页聚合接口 `/home/data` 外，所有列表接口均使用此分页约定。

### 鉴权说明

需要登录的接口通过 **JWT Token** 鉴权，请求头携带：

```
Authorization: Bearer <access_token>
```

Token 通过 `/auth/login` 系列接口获取，过期后使用 `/auth/refreshToken` 刷新。

### 时间格式

- 响应中的时间字段均为 **UTC ISO8601** 格式，如 `"2026-07-07T06:58:00Z"`
- 前端需自行转换为本地时区显示

---

## 通用数据模型

### 商品对象（Product）

```json
{
  "id": 18,
  "code": "iphone-17-pro",
  "name": "iPhone 17 Pro",
  "description": "商品描述...",
  "price": 799900,
  "sale_price": 789900,
  "stock": 100,
  "category_id": 2,
  "status": 1,
  "main_image": "https://...",
  "images": ["https://...", "https://..."],
  "detail_html": "<p>富文本详情...</p>",
  "sales": 1024,
  "sort": 10,
  "brand": "Apple",
  "favorite_count": 56,
  "category_name": "手机",
  "tags": [
    { "id": 7, "name": "热门推荐", "color": "#f18383", "weight": 100 }
  ],
  "create_time": "2026-07-01T08:00:00Z",
  "update_time": "2026-07-10T12:00:00Z"
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 商品ID |
| code | String | 商品业务ID（唯一标识，URL 中使用） |
| name | String | 商品名称 |
| description | String | 商品描述 |
| price | Integer | 原价（单位：分） |
| sale_price | Integer | 销售价（单位：分） |
| stock | Integer | 库存数量 |
| category_id | Integer | 所属分类ID |
| status | Integer | 状态：0-下架，1-上架 |
| main_image | String | 主图URL |
| images | Array\<String\> | 图片列表 |
| detail_html | String | 商品详情（富文本HTML） |
| sales | Integer | 销量 |
| sort | Integer | 排序权重 |
| brand | String | 品牌 |
| favorite_count | Integer | 收藏数量 |
| category_name | String | 分类名称（关联字段） |
| tags | Array | 标签列表（含 id、name、color、weight） |
| create_time | ISO8601 | 创建时间（UTC） |
| update_time | ISO8601 | 更新时间（UTC） |

---

## 商品模块

> **前缀**: `/api/v1/product`
> **无鉴权要求** — 商品信息属于公开数据

### 1. 获取商品详情

**接口地址**: `GET /api/v1/product/<product_code>`

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| product_code | String | 商品业务ID（必填），自动去除首尾空格并转为小写 |

**请求示例**:

```javascript
fetch('/api/v1/product/iphone-17-pro')
  .then(res => res.json())
  .then(data => console.log(data));
```

**响应格式**:

```json
{
  "code": 200,
  "message": "获取商品详情成功",
  "data": { /* 商品对象 Product */ }
}
```

**缓存说明**: 支持 Redis 缓存，TTL 默认 120 秒

**错误响应**:

```json
{ "code": 404, "message": "商品不存在或已下架" }
```

---

### 2. 商品搜索

**接口地址**: `GET /api/v1/product/search`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |
| keyword | String | '' | 关键字（商品名称模糊匹配） |
| category_id | Integer | - | 分类ID筛选 |
| min_price | Float | - | 最低价格 |
| max_price | Float | - | 最高价格 |
| sort_by | String | `create_time` | 排序字段：`create_time` \| `price` \| `sales` |
| sort_order | String | `desc` | 排序方向：`asc` \| `desc`（`sales` 始终降序） |

**请求示例**:

```javascript
// 搜索手机类商品，按价格升序
fetch('/api/v1/product/search?keyword=iPhone&category_id=2&sort_by=price&sort_order=asc')
  .then(res => res.json())
  .then(data => console.log(data));
```

**响应格式**:

```json
{
  "code": 200,
  "message": "搜索商品成功",
  "data": {
    "products": [ /* 商品对象数组 */ ],
    "pagination": {
      "page": 1,
      "pageSize": 10,
      "total": 50,
      "pages": 5
    }
  }
}
```

---

### 3. 获取商品分类树

**接口地址**: `GET /api/v1/product/category/list`

**请求参数**: 无

**请求示例**:

```javascript
fetch('/api/v1/product/category/list')
  .then(res => res.json())
  .then(data => console.log(data));
```

**响应格式**:

```json
{
  "code": 200,
  "message": "获取分类列表成功",
  "data": [
    {
      "id": 1,
      "name": "手机数码",
      "imageUrl": "https://...",
      "children": [
        { "id": 11, "name": "智能手机", "imageUrl": "...", "children": [] },
        { "id": 12, "name": "耳机", "imageUrl": "...", "children": [] }
      ]
    }
  ]
}
```

---

### 4. 获取分类下的商品

**接口地址**: `GET /api/v1/product/category/<category_id>/products`

**请求参数**:

| 参数 | 位置 | 类型 | 默认值 | 说明 |
|------|------|------|--------|------|
| category_id | 路径 | Integer | - | 分类ID（必填） |
| page | Query | Integer | 1 | 页码 |
| pageSize | Query | Integer | 10 | 每页数量 |
| sort_by | Query | String | `create_time` | 排序：`create_time` \| `sales` \| `price` |
| sort_order | Query | String | `desc` | 方向：`asc` \| `desc` |

**请求示例**:

```javascript
fetch('/api/v1/product/category/2/products?page=1&pageSize=10&sort_by=price&sort_order=asc')
  .then(res => res.json())
  .then(data => console.log(data));
```

**响应格式**:

```json
{
  "code": 200,
  "message": "获取分类商品成功",
  "data": {
    "category_name": "手机",
    "products": [ /* 商品对象数组 */ ],
    "pagination": {
      "page": 1,
      "pageSize": 10,
      "total": 25,
      "pages": 3
    }
  }
}
```

---

## 购物车模块

> **前缀**: `/api/v1/cart`
> **需要鉴权** — 所有接口

### 1. 获取购物车列表

**接口地址**: `GET /api/v1/cart/list`

**请求参数**: 无（从登录态获取当前用户）

**响应格式**: `[ /* 购物车项，含商品对象 */ ]` + `total_price`

---

### 2. 加入购物车

**接口地址**: `POST /api/v1/cart/add`

**请求体**:

```json
{
  "product_id": 18,
  "quantity": 1
}
```

---

### 3. 更新购物车项数量

**接口地址**: `PUT /api/v1/cart/update/<cart_item_id>`

**请求体**:

```json
{
  "quantity": 3
}
```

---

### 4. 删除购物车项

**接口地址**: `DELETE /api/v1/cart/del/<cart_item_id>`

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| cart_item_id | Integer | 购物车项ID |

---

### 5. 清空购物车

**接口地址**: `DELETE /api/v1/cart/clear`

---

## 认证模块

> **前缀**: `/api/v1/auth`
> **无鉴权要求**

### 1. 微信登录

**接口地址**: `POST /api/v1/auth/wxLogin`

**请求体**:

```json
{
  "code": "微信wx.login()获取的code",
  "nickname": "用户昵称",
  "avatar": "头像URL"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "access_token": "eyJhbG...",
    "refresh_token": "eyJhbG...",
    "user": { /* 用户信息 */ }
  }
}
```

---

### 2. 手机号登录

**接口地址**: `POST /api/v1/auth/loginByPhone`

**请求体**:

```json
{
  "phone": "1*********0",
  "code": "123456"
}
```

---

### 3. 用户名登录

**接口地址**: `POST /api/v1/auth/loginByUsername`

**请求体**:

```json
{
  "username": "testuser",
  "password": "******"
}
```

---

### 4. 通用登录

**接口地址**: `POST /api/v1/auth/login`

支持多种账号体系（微信 / 手机 / 用户名），根据请求体字段自动识别登录方式。

---

### 5. 注册

**接口地址**: `POST /api/v1/auth/register`

**请求体**:

```json
{
  "username": "newuser",
  "password": "******",
  "phone": "1*********0",
  "code": "123456"
}
```

---

### 6. 刷新 Token

**接口地址**: `POST /api/v1/auth/refreshToken`

**鉴权要求**: 需要在请求头中携带 `refresh_token`（不是放在请求体）

```http
Authorization: Bearer <refresh_token>
```

**响应格式**:

```json
{
  "code": 200,
  "message": "Token刷新成功",
  "data": {
    "access_token": "eyJhbG...",
    "token_type": "Bearer"
  },
  "success": true
}
```

---

### 7. 登出

**接口地址**: `GET /api/v1/auth/logout`

**需要鉴权**

---

### 8. 获取验证码

**接口地址**: `GET /api/v1/auth/getCode`

**请求参数** (Query):

| 参数 | 类型 | 说明 |
|------|------|------|
| phone | String | 手机号 |

---

## 订单模块(暂不对接)

> **前缀**: `/api/v1`
> **需要鉴权**

### 1. 创建订单

**接口地址**: `POST /api/v1/orders`

**请求体**:

```json
{
  "address_id": 5,
  "items": [
    { "product_id": 18, "quantity": 1 },
    { "product_id": 17, "quantity": 2 }
  ],
  "remark": "请尽快发货"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "创建订单成功",
  "data": {
    "order_id": 1001,
    "order_no": "202607160001",
    "total_amount": 1999.00
  }
}
```

---

### 2. 获取订单列表

**接口地址**: `GET /api/v1/orders`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |
| status | Integer | - | 订单状态筛选：0-待支付 1-已支付 2-已发货 3-已完成 4-已取消 |

**响应格式**:

```json
{
  "code": 200,
  "message": "获取订单列表成功",
  "data": {
    "orders": [ /* 订单对象数组 */ ],
    "pagination": { "page": 1, "pageSize": 10, "total": 5, "pages": 1 }
  }
}
```

---

### 3. 获取订单详情

**接口地址**: `GET /api/v1/orders/<order_id>`

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| order_id | Integer | 订单ID |

**响应格式**: 订单完整信息 + 订单项列表（含商品快照）

---

### 4. 取消订单

**接口地址**: `POST /api/v1/orders/<order_id>/cancel`

---

### 5. 支付订单

**接口地址**: `POST /api/v1/orders/<order_id>/pay`

**请求体**:

```json
{
  "payment_method": "wechat",
  "amount": 1999.00
}
```

---

### 6. 确认收货

**接口地址**: `POST /api/v1/orders/<order_id>/confirm`

---

## 收藏模块

> **前缀**: `/api/v1`
> **需要鉴权**

### 1. 收藏 / 取消收藏（切换）

**接口地址**: `POST /api/v1/favorite/toggle`

**请求体**:

```json
{
  "item_type": "product",
  "item_id": 18
}
```

| 参数 | 类型 | 说明 |
|------|------|------|
| item_type | String | 收藏类型：`product`（商品） \| `article`（文章） |
| item_id | Integer | 商品/文章ID |

**响应格式**:

```json
{
  "code": 200,
  "message": "收藏成功",
  "data": {
    "is_favorited": true
  }
}
```

---

### 2. 检查是否已收藏

**接口地址**: `POST /api/v1/favorite/check`

**请求体**: 同 toggle

**响应格式**:

```json
{
  "code": 200,
  "data": {
    "is_favorited": true
  }
}
```

---

### 3. 获取收藏列表

**接口地址**: `GET /api/v1/favorite/list`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |
| item_type | String | - | 筛选：`product` \| `article`，不传返回全部 |

**响应格式**:

```json
{
  "code": 200,
  "data": {
    "favorites": [
      {
        "id": 1,
        "item_type": "product",
        "item_id": 18,
        "item_title": "iPhone 17 Pro",
        "item_image": "https://...",
        "item_description": "...",
        "created_at": "2026-07-16T08:00:00Z"
      }
    ],
    "pagination": { "page": 1, "pageSize": 10, "total": 5, "pages": 1 }
  }
}
```

---

### 4. 获取收藏数量

**接口地址**: `GET /api/v1/favorite/count`

**请求参数** (Query):

| 参数 | 类型 | 说明 |
|------|------|------|
| item_type | String | 可选，不传返回总数 |

---

### 5. 移除收藏

**接口地址**: `POST /api/v1/favorite/remove`

**请求体**:

```json
{
  "item_type": "product",
  "item_id": 18
}
```

---

### 6. 清空收藏

**接口地址**: `POST /api/v1/favorite/clear`

**请求体**:

```json
{
  "item_type": "product"
}
```

不传 `item_type` 则清空全部收藏。

---

## 评论模块

> **前缀**: `/api/v1/comments`

### 1. 发表评论

**接口地址**: `POST /api/v1/comments/create`

**需要鉴权**

**请求体**:

```json
{
  "article_id": 10,
  "content": "写得真好！",
  "parent_id": 0
}
```

| 参数 | 类型 | 说明 |
|------|------|------|
| article_id | Integer | 文章ID |
| content | String | 评论内容 |
| parent_id | Integer | 父评论ID，0 表示一级评论 |

**响应格式**: 返回完整评论对象

---

### 2. 获取文章评论列表

**接口地址**: `GET /api/v1/comments/article/<article_id>`

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| article_id | Integer | 文章ID |

**响应格式**:

```json
{
  "code": 200,
  "data": {
    "comments": [
      {
        "id": 1,
        "article_id": 10,
        "user_id": 5,
        "user_nickname": "昵称",
        "user_avatar": "https://...",
        "content": "评论内容",
        "parent_id": 0,
        "level": 1,
        "like_count": 3,
        "reply_count": 2,
        "created_at": "2026-07-16T08:00:00Z",
        "children": [ /* 子评论递归 */ ]
      }
    ],
    "total": 15
  }
}
```

---

### 3. 评论点赞

**接口地址**: `POST /api/v1/comments/<comment_id>/like`

**需要鉴权**

**响应格式**:

```json
{
  "code": 200,
  "data": {
    "is_liked": true,
    "like_count": 4
  }
}
```

---

### 4. 删除评论

**接口地址**: `DELETE /api/v1/comments/<comment_id>/delete`

**需要鉴权**（仅评论作者或管理员可删除）

---

### 5. 获取用户评论列表

**接口地址**: `GET /api/v1/comments/user/<user_id>`

---

### 6. 获取文章评论统计

**接口地址**: `GET /api/v1/comments/statistics/<article_id>`

---

## 文章模块

> **前缀**: `/api/v1/article`
> **无鉴权要求**

### 1. 获取文章列表

**接口地址**: `GET /api/v1/article/list`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |
| category_id | Integer | - | 分类筛选 |
| keyword | String | - | 关键字搜索 |
| sort_by | String | published_date | 排序字段：published_date / views / likes / comment_count |
| order | String | desc | 排序方向：desc / asc |

**响应格式**:

```json
{
  "code": 200,
  "data": {
    "articles": [ /* 文章对象数组 */ ],
    "pagination": {
      "page": 1,
      "pageSize": 10,
      "total": 50,
      "pages": 5
    }
  }
}
```

**文章对象字段**（`Article.to_dict()`）:

```json
{
  "id": 10,
  "code": "art_abc123",
  "user_id": 5,
  "title": "文章标题",
  "content": "文章内容（HTML）",
  "image": "https://...封面图URL（已解析）",
  "description": "摘要",
  "status": 1,
  "author": {
    "id": 5,
    "nickname": "作者昵称（别名优先）",
    "avatar": "https://...头像URL",
    "description": "作者简介",
    "alias": "管理员设置的别名（可能为 null）"
  },
  "category": {
    "id": 3,
    "name": "技术"
  },
  "views": 1024,
  "likes": 56,
  "comment_count": 12,
  "favorite_count": 8,
  "tags": [ { "id": 1, "name": "Python", "color": "#3776ab", "weight": 100, "type": 2 } ],
  "published_date": "2026-07-01T08:00:00Z",
  "update_date": "2026-07-10T12:00:00Z"
}
```

**字段说明**:

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | Integer/null | 关联用户ID，可为 null（匿名文章） |
| author | Object/null | 作者信息对象，无作者时为 null |
| author.nickname | String | 显示名称：别名 > 用户昵称 > 用户名 |
| author.alias | String/null | 管理员设置的别名 |
| category | Object/null | 分类信息对象 |
| image | String | 已解析的完整图片URL |

---

### 2. 获取文章详情

**接口地址**: `GET /api/v1/article/<article_code>`

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| article_code | String | 文章业务编码（如 `art_abc123`） |

**响应格式**: 单篇文章对象 + `comment_statistics`（评论统计）

```json
{
  "code": 200,
  "data": {
    "id": 10,
    "code": "art_abc123",
    "title": "文章标题",
    "content": "文章内容（HTML）",
    "author": { "id": 5, "nickname": "作者", "avatar": "...", "alias": null },
    "category": { "id": 3, "name": "技术" },
    "comment_statistics": {
      "total_comments": 15,
      "root_comments": 10,
      "reply_comments": 5,
      "total_likes": 30
    }
  }
}
```

---

### 3. 获取相关文章

**接口地址**: `GET /api/v1/article/<article_code>/related`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| pageSize | Integer | 5 | 返回数量 |

---

### 4. 获取文章分类

**接口地址**: `GET /api/v1/article/categories`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| include_count | String | false | 是否包含文章数量 |

**响应格式**:

```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "name": "技术",
      "parent_id": null,
      "is_displayed": 1,
      "sort_order": 0,
      "children": []
    }
  ]
}
```

---

### 5. 获取文章标签

**接口地址**: `GET /api/v1/article/tags`

**请求参数** (Query):

| 参数 | 类型 | 说明 |
|------|------|------|
| type | Integer | 标签类型筛选：1-商品 2-文章 3-通用 |

---

### 6. 获取热门文章

**接口地址**: `GET /api/v1/article/hot`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| pageSize | Integer | 10 | 返回数量 |

按热度分数排序（阅读量×0.1 + 点赞数×2 + 评论数×3）。

---

### 7. 获取最新文章

**接口地址**: `GET /api/v1/article/latest`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| pageSize | Integer | 10 | 返回数量 |

按发布时间排序。

---

### 8. 文章搜索

**接口地址**: `GET /api/v1/article/search`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| keyword | String | - | 搜索关键字（标题/内容/描述模糊匹配） |
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |

---

## 用户信息模块

> **前缀**: `/api/v1/user`
> **需要鉴权**

### 1. 获取用户信息

**接口地址**: `GET /api/v1/user/info`

**响应格式**:

```json
{
  "code": 200,
  "data": {
    "id": 5,
    "username": "testuser",
    "nickname": "测试用户",
    "phone": "138****8888",
    "gender": 1,
    "avatar": "https://...",
    "description": "个性签名",
    "enable": 1,
    "birthday": "1990-01-01",
    "create_at": "2025-01-01T08:00:00Z"
  }
}
```

---

### 2. 更新用户信息

**接口地址**: `POST` 或 `PUT /api/v1/user/updateInfo`

**请求体**（所有字段可选，仅更新传入的字段）:

```json
{
  "nickname": "新昵称",
  "gender": 1,
  "birthday": "1995-05-20",
  "description": "更新了签名"
}
```

---

### 3. 修改密码

**接口地址**: `POST /api/v1/user/updatePassword`

**请求体**:

```json
{
  "old_password": "原密码",
  "new_password": "新密码"
}
```

---

### 4. 获取个人资料

**接口地址**: `GET /api/v1/user/profile`

与 `/info` 类似，返回更完整的资料信息。

---

### 5. 绑定手机号

**接口地址**: `POST /api/v1/user/bindPhone`

**请求体**:

```json
{
  "phone": "1*********0",
  "code": "123456"
}
```

---

### 6. 更新背景封面

**接口地址**: `POST /api/v1/user/bgCover`

**请求体**:

```json
{
  "bg_cover": "https://..."
}
```

支持 FormData 上传图片或传 URL。

---

### 7. 获取用户公开详情

**接口地址**: `GET /api/v1/user/<user_id>`

**说明**: 获取指定用户的公开信息（用于查看文章作者主页等场景），无需鉴权。

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| user_id | Integer | 用户ID |

**响应格式**:

```json
{
  "code": 200,
  "data": {
    "id": 5,
    "username": "testuser",
    "nickname": "测试用户",
    "gender": 1,
    "avatar": "https://...头像URL",
    "bg_cover": "https://...背景封面URL",
    "description": "个性签名",
    "create_at": "2025-01-01T08:00:00Z"
  }
}
```

> **注意**: 仅返回公开信息，不包含 phone、openid、unionid 等敏感字段。

---

## 地址管理模块

> **前缀**: `/api/v1/user/addresses`
> **需要鉴权**

### 1. 获取地址列表

**接口地址**: `GET /api/v1/user/addresses`

**响应格式**:

```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "recipient_name": "张三",
      "phone": "1*********0",
      "address_line": "xx省xx市xx区xx路xx号",
      "is_default": true
    }
  ]
}
```

---

### 2. 新增地址

**接口地址**: `POST /api/v1/user/addresses`

**请求体**:

```json
{
  "recipient_name": "张三",
  "phone": "1*********0",
  "address_line": "详细地址",
  "is_default": false
}
```

---

### 3. 编辑地址

**接口地址**: `PUT /api/v1/user/addresses/<address_id>`

**请求体**: 同新增（所有字段可选）

---

### 4. 删除地址

**接口地址**: `DELETE /api/v1/user/addresses/<address_id>`

---

### 5. 设置默认地址

**接口地址**: `PUT /api/v1/user/addresses/<address_id>/setDefault`

---

### 6. 获取默认地址

**接口地址**: `GET /api/v1/user/addresses/default`

---

## 首页模块

> **前缀**: `/api/v1`
> **无鉴权要求**

### 1. 获取首页聚合数据

**接口地址**: `GET /api/v1/home/data`

**请求参数**: 无

**响应格式**:

```json
{
  "code": 200,
  "message": "获取首页数据成功",
  "data": {
    "banners": [ /* 轮播图 */ ],
    "categories": [ /* 一级分类 */ ],
    "featured": {
      "tag_name": "精选推荐",
      "products": [ /* 商品 */ ]
    },
    "hot_products": [ /* 热门商品 */ ],
    "articles": [ /* 推荐文章 */ ],
    "new_products": [ /* 新品推荐 */ ]
  },
  "source": "cache",
  "cache_key": "home/data"
}
```

**缓存说明**: 支持 Redis 缓存，TTL 默认 180 秒。`source` 字段表明数据来源（`cache` 或 `database`）。

---

### 2. 获取 Banner 轮播图

**接口地址**: `GET /api/v1/banners`

**请求参数** (Query):

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| placement_key | String | 是 | 位置标识：`home_top_banner` \| `home_middle_ad` \| `mall_top_banner` \| `product_detail_ad` 等 |
| client_type | String | 否 | 终端：`all` \| `web` \| `miniapp` \| `app`，默认 `all` |

**请求示例**:

```javascript
fetch('/api/v1/banners?placement_key=mall_top_banner&client_type=miniapp')
  .then(res => res.json())
  .then(data => console.log(data));
```

**响应格式**:

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "banners": [
      {
        "id": 1,
        "title": "首页轮播",
        "image_url": "https://...",
        "placement_key": "mall_top_banner",
        "link_type": "PRODUCT",
        "link_target": "18",
        "client_type": "all",
        "sort_order": 10,
        "status": true
      }
    ],
    "source": "database",
    "cache_key": "banner:mall_top_banner:miniapp",
    "cached": true
  }
}
```

**缓存说明**: 支持 Redis 缓存，TTL 默认 3600 秒。

**错误响应**:

```json
{
  "code": 400,
  "message": "缺少必要参数: placement_key",
  "data": []
}
```

---

### 3. 获取公告列表

**接口地址**: `GET /api/v1/announcements`

**请求参数** (Query):

| 参数 | 类型 | 说明 |
|------|------|------|
| position | String | 展示位置筛选：`homepage` \| `user_center` \| `product_page` |
| terminal | String | 终端筛选 |
| announcement_type | String | 公告类型筛选 |
| force_only | Boolean | 仅返回强提醒公告 |

**响应格式**:

```json
{
  "code": 200,
  "message": "获取公告成功",
  "data": [
    {
      "id": 1,
      "title": "系统维护通知",
      "announcement_type": "system_notice",
      "content": "...",
      "display_positions": ["homepage"],
      "force_reminder": false,
      "terminals": ["web", "miniapp"],
      "effective_time": "2026-07-01T00:00:00Z",
      "end_time": "2026-07-31T23:59:59Z",
      "created_at": "2026-06-15T08:00:00Z"
    }
  ]
}
```

---

### 4. 获取首页商品分类

**接口地址**: `GET /api/v1/home/categories`

与商品模块的 `/category/list` 不同，此接口额外返回每个分类下的 `product_count`（商品数量）。

**响应格式**:

```json
{
  "code": 200,
  "message": "获取分类数据成功",
  "data": [
    {
      "id": 1,
      "name": "手机数码",
      "image": "https://...",
      "product_count": 25,
      "children": [
        { "id": 11, "name": "智能手机", "image": "...", "product_count": 10 }
      ]
    }
  ]
}
```

---

### 5. 获取精选商品

**接口地址**: `GET /api/v1/home/featured-products`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |

**响应格式**:

```json
{
  "code": 200,
  "message": "获取精选商品成功",
  "data": {
    "tag_name": "热门推荐",
    "tag_color": "#f18383",
    "products": [ /* 商品对象数组（完整 to_dict） */ ],
    "pagination": { "page": 1, "pageSize": 10, "total": 6, "pages": 1 }
  }
}
```

---

### 6. 获取热门商品

**接口地址**: `GET /api/v1/home/hot-products`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |

按销量降序排列。

**响应格式**:

```json
{
  "code": 200,
  "message": "获取热门商品成功",
  "data": {
    "products": [ /* 商品对象数组（完整 to_dict） */ ],
    "pagination": { "page": 1, "pageSize": 10, "total": 8, "pages": 1 }
  }
}
```

---

### 7. 获取推荐文章

**接口地址**: `GET /api/v1/home/articles`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |
| category_id | Integer | - | 分类筛选 |

---

### 8. 获取新品推荐

**接口地址**: `GET /api/v1/new-products`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | Integer | 1 | 页码 |
| pageSize | Integer | 10 | 每页数量 |

按创建时间降序。每个商品对象额外包含 `is_new: true` 标记。

---

### 9. 获取标签列表

**接口地址**: `GET /api/v1/tags`

**请求参数** (Query):

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | Integer | 1 | 标签类型：1-商品 2-文章 3-通用 |

**响应格式**:

```json
{
  "code": 200,
  "message": "获取标签列表成功",
  "data": [
    {
      "id": 7,
      "name": "热门推荐",
      "color": "#f18383",
      "weight": 100,
      "type": 1,
      "description": ""
    }
  ]
}
```

---

## 地区角色模块（暂不对接）

> **前缀**: `/api/v1`
> **无鉴权要求**

该模块为游戏 / 内容平台的地区与角色数据，供前端展示地区-角色层级关系。

### 1. 获取地区列表

**接口地址**: `GET /api/v1/regions`

**请求参数** (Query):

| 参数 | 类型 | 说明 |
|------|------|------|
| parent_id | Integer | 父级地区ID，不传返回顶级地区 |

**响应格式**: 地区对象嵌套树形结构，每个节点含 `children`。

---

### 2. 获取地区详情

**接口地址**: `GET /api/v1/regions/<region_code>`

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| region_code | String | 地区编码（如 `cn-east`） |

---

### 3. 获取角色列表

**接口地址**: `GET /api/v1/characters`

**请求参数** (Query):

| 参数 | 类型 | 说明 |
|------|------|------|
| region_code | String | 按地区筛选 |
| page | Integer | 页码 |
| pageSize | Integer | 每页数量 |

---

### 4. 获取角色详情

**接口地址**: `GET /api/v1/characters/<character_code>`

**URL 参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| character_code | String | 角色编码 |

---

## 附录

### 接口总览表

| 模块 | 蓝图前缀 | 接口数 | 鉴权 |
|------|----------|--------|------|
| 商品 | `/api/v1/product` | 4 | 否 |
| 购物车 | `/api/v1/cart` | 5 | 是 |
| 认证 | `/api/v1/auth` | 8 | 否（logout 除外） |
| 订单 | `/api/v1` | 6 | 是 |
| 收藏 | `/api/v1` | 6 | 是 |
| 评论 | `/api/v1/comments` | 6 | 部分 |
| 文章 | `/api/v1/article` | 8 | 否 |
| 用户信息 | `/api/v1/user` | 6 | 是 |
| 地址管理 | `/api/v1/user/addresses` | 6 | 是 |
| 首页 | `/api/v1` | 9 | 否 |
| 地区角色 | `/api/v1` | 4 | 否 |
| **合计** | | **68** | |

### 前后端协作注意事项

1. **价格单位**：商品价格为"分"（整数），前端展示时需除以 100 转换为"元"。
2. **时间处理**：所有时间字段为 UTC，前端须自行转换本地时区。
3. **Token 刷新**：access_token 过期时先调 `/auth/refreshToken`，refresh_token 也失效时需重新登录。
4. **列表字段一致性**：2026-07 起商品类列表接口（搜索、分类、首页等）统一返回 `to_dict()` 完整字段，含 `sale_price`、`status`、`sort`、`brand`、`tags`、`create_time`、`update_time`、`favorite_count`、`main_image`、`images`、`detail_html`。
5. **分页参数已统一**：所有列表接口统一使用 `page` / `pageSize` 作为请求参数，响应中通过 `pagination.{page, pageSize, total, pages}` 返回分页信息。
6. **错误处理**：请优先以响应体中的 `code` / `message` 判定业务结果。历史接口中存在“HTTP 状态码为 200 但 `code` 为非 200”的兼容行为，建议前端统一按业务码拦截。

### 文档更新记录

- 2026-07-25：校对并修正与后端实现一致性问题（`refreshToken` 鉴权方式、认证返回示例字段、文章详情/相关文章参数由 `article_id` 更正为 `article_code`、错误处理约定补充）。

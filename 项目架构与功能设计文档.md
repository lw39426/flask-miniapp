# 项目架构与功能设计文档

## 一、项目概述

### 1.1 项目简介
这是一个基于 **uni-app** 框架开发的跨平台电商小程序项目，采用现代化的前端技术栈构建。项目定位为一个功能完善的电商应用，支持商品浏览、购物车、收藏、评论、聊天客服等核心电商功能。

### 1.2 技术栈
- **框架**: uni-app 3.x (支持 H5、小程序、APP 多端)
- **前端框架**: Vue 3.4+ (Composition API)
- **开发语言**: TypeScript 5.8+
- **构建工具**: Vite 5.2+
- **样式方案**: UnoCSS + SCSS
- **UI 组件库**: sard-uniapp + 自定义组件
- **状态管理**: Pinia 2.0+ (支持持久化)
- **HTTP 请求**: 
  - 自封装 http 模块 (基于 uni.request)
  - Alova (可选的请求库)
- **WebSocket**: Socket.IO Client (@hyoga/uni-socket.io)
- **路由管理**: @uni-helper/vite-plugin-uni-pages (约定式路由)
- **代码规范**: ESLint + Husky + Commitlint

### 1.3 支持平台
| 平台 | 支持状态 | 说明 |
|------|---------|------|
| H5 | ✅ | 完全支持 |
| 微信小程序 | ✅ | 完全支持 |
| APP (iOS/Android) | ✅ | 完全支持 |
| 支付宝小程序 | ✅ | 完全支持 |
| 其他小程序 | ✅ | 字节、快手、百度等 |

---

## 二、目录结构解析

### 2.1 整体目录结构

```
项目根目录/
├── .cursor/              # Cursor AI 编辑器配置
│   └── rules/           # 项目开发规则文档
├── .husky/              # Git Hooks 配置
├── .vscode/             # VSCode 编辑器配置
├── dist/                # 构建输出目录
├── env/                 # 环境变量配置文件
├── md/                  # 项目文档
├── scripts/             # 构建脚本
├── src/                 # 源代码目录 ⭐核心
│   ├── api/            # API 接口定义
│   ├── components/     # 全局组件
│   ├── hooks/          # 组合式函数
│   ├── http/           # HTTP 请求封装
│   ├── pages/          # 页面文件
│   ├── pages-sub/      # 分包页面
│   ├── router/         # 路由配置
│   ├── service/        # 业务服务层
│   ├── static/         # 静态资源
│   ├── store/          # 状态管理
│   ├── style/          # 全局样式
│   ├── tabbar/         # 底部导航栏
│   ├── types/          # TypeScript 类型定义
│   ├── utils/          # 工具函数
│   ├── App.vue         # 应用入口组件
│   ├── main.ts         # 应用入口文件
│   ├── pages.json      # 页面配置
│   └── manifest.json   # 应用配置清单
├── package.json         # 项目依赖配置
├── vite.config.ts      # Vite 构建配置
├── tsconfig.json       # TypeScript 配置
├── uno.config.ts       # UnoCSS 配置
└── README.md           # 项目说明文档
```

### 2.2 核心目录职责说明

#### 📁 src/api/ - API 接口层
**职责**: 定义所有后端 API 接口调用
- `article.ts` - 文章相关接口
- `cart.ts` - 购物车接口
- `category.ts` - 分类接口
- `chat.ts` - 聊天接口
- `comment.ts` - 评论接口
- `favorite.ts` - 收藏接口
- `login.ts` - 登录认证接口
- `home.ts` - 首页数据接口
- `types/` - 接口类型定义

**特点**: 
- 统一的接口返回类型 `ApiResponse<T>`
- 完善的 TypeScript 类型定义
- 支持请求/响应拦截器


#### 📁 src/http/ - HTTP 请求封装层
**职责**: 封装统一的 HTTP 请求逻辑
- `http.ts` - 核心请求封装 (基于 uni.request)
- `interceptor.ts` - 请求/响应拦截器
- `alova.ts` - Alova 请求库配置 (可选)
- `types.ts` - 请求相关类型定义
- `tools/` - 工具函数 (枚举、查询字符串处理)

**核心功能**:
1. **自动 Token 注入**: 拦截器自动添加 Authorization 头
2. **Token 刷新机制**: 支持双 Token 无感刷新
3. **错误统一处理**: 401/429 等状态码统一处理
4. **请求队列**: Token 刷新时暂存请求队列
5. **环境适配**: 自动处理 H5 代理和小程序直连

#### 📁 src/store/ - 状态管理层
**职责**: 全局状态管理 (基于 Pinia)
- `token.ts` - Token 和认证状态管理 ⭐
- `user.ts` - 用户信息管理
- `chat.ts` - 聊天会话管理
- `theme.ts` - 主题配置管理
- `index.ts` - Store 统一导出

**核心 Store 说明**:

**1. tokenStore (认证核心)**
```typescript
// 核心状态
- tokenInfo: 单Token/双Token 信息
- hasLogin: 是否已登录 (计算属性)
- isTokenExpired: Token 是否过期

// 核心方法
- login() - 用户名密码登录
- loginByPhone() - 手机号登录
- wxLogin() - 微信登录
- alipayLogin() - 支付宝登录
- register() - 用户注册
- logout() - 退出登录
- refreshToken() - 刷新 Token (双Token模式)
```

**2. userStore (用户信息)**
```typescript
// 核心状态
- userInfo: 用户详细信息 (头像、昵称等)

// 核心方法
- fetchUserInfo() - 获取用户信息
- updateUserInfo() - 更新用户信息
- setUserAvatar() - 设置用户头像
- clearUserInfo() - 清除用户信息
```

**3. chatStore (聊天管理)**
```typescript
// 核心状态
- conversations: 会话列表
- userOnlineStatus: 用户在线状态 Map
- totalUnreadCount: 总未读数

// 核心方法
- fetchConversations() - 加载会话列表
- updateUserOnlineStatus() - 更新用户在线状态
- updateConversationItem() - 更新会话项
- markConversationAsRead() - 标记已读
```


#### 📁 src/pages/ - 页面文件
**职责**: 应用的所有页面组件

**主要页面模块**:
```
pages/
├── index/           # 首页模块
│   ├── index.vue   # 首页主页面
│   └── components/ # 首页组件 (骨架屏等)
├── login/           # 登录模块
│   ├── login.vue   # 登录页
│   ├── register.vue # 注册页
│   └── forgot-password.vue # 忘记密码
├── my/              # 个人中心模块
│   ├── my.vue      # 个人中心首页
│   ├── profile.vue # 个人资料
│   ├── favorite.vue # 我的收藏
│   └── setting.vue # 设置页
├── cart/            # 购物车模块
│   ├── cart.vue    # 购物车页面
│   └── components/ # 购物车组件
├── category/        # 分类模块
│   ├── category.vue # 分类页面
│   └── components/ # 分类组件
├── chat/            # 聊天模块 ⭐
│   ├── chat.vue    # 会话列表
│   ├── room.vue    # 聊天室
│   └── config.ts   # 聊天配置
├── product/         # 商品模块
│   ├── detail.vue  # 商品详情
│   └── list.vue    # 商品列表
├── article/         # 文章模块
│   └── detail.vue  # 文章详情
└── search/          # 搜索模块
    └── index.vue   # 搜索页面
```

#### 📁 src/components/ - 全局组件
**职责**: 可复用的全局组件
- `CommentSystem.vue` - 评论系统组件
- `CommentItem.vue` - 评论项组件
- `CategoryProducts.vue` - 分类商品组件
- `NavBarSearch.vue` - 导航栏搜索组件
- `GlobalLoading.vue` - 全局加载组件
- `CustomPreview.vue` - 自定义预览组件
- `skeleton/` - 骨架屏组件

#### 📁 src/utils/ - 工具函数层
**职责**: 通用工具函数
- `socket.ts` - WebSocket 管理器 ⭐
- `cart.ts` - 购物车工具函数
- `platform.ts` - 平台判断工具
- `systemInfo.ts` - 系统信息获取
- `uploadFile.ts` - 文件上传工具
- `index.ts` - 通用工具函数集合

**核心工具 - socketManager**:
```typescript
// WebSocket 单例管理器
class SocketIOManager {
  // 核心方法
  init(token) - 初始化连接
  connect(token) - 建立连接
  disconnect() - 断开连接
  destroy() - 销毁实例
  send(event, data) - 发送消息
  on(event, handler) - 监听事件
  off(event, handler) - 移除监听
  
  // 聊天相关
  joinConversationRoom(roomId) - 加入会话房间
  leaveConversationRoom(roomId) - 离开会话房间
  sendMessage(roomId, content) - 发送消息
  sendTyping(roomId, isTyping) - 发送输入状态
}
```


#### 📁 src/router/ - 路由管理
**职责**: 路由拦截和登录鉴权
- `interceptor.ts` - 路由拦截器 ⭐
- `config.ts` - 路由配置 (登录页、白名单等)

**路由拦截逻辑**:
1. 检查用户登录状态
2. 判断目标页面是否在白名单
3. 未登录且需要登录 → 跳转登录页
4. 已登录访问登录页 → 跳转首页
5. 支持黑白名单两种策略

#### 📁 src/tabbar/ - 底部导航栏
**职责**: 自定义 TabBar 实现
- `index.vue` - TabBar 组件
- `config.ts` - TabBar 配置 ⭐
- `store.ts` - TabBar 状态管理

**TabBar 策略**:
```typescript
// 支持 4 种策略
0: NO_TABBAR - 无 TabBar
1: NATIVE_TABBAR - 原生 TabBar
2: CUSTOM_TABBAR_WITH_CACHE - 自定义 TabBar (带缓存)
3: CUSTOM_TABBAR_WITHOUT_CACHE - 自定义 TabBar (无缓存)
```

**TabBar 配置示例**:
```typescript
customTabbarList: [
  { text: '首页', pagePath: 'pages/index/index', icon: 'i-carbon-home' },
  { text: '分类', pagePath: 'pages/category/category', icon: 'i-carbon-category' },
  { text: '客服', pagePath: 'pages/chat/chat', isBulge: true }, // 鼓包按钮
  { text: '购物车', pagePath: 'pages/cart/cart', icon: 'i-carbon-shopping-cart' },
  { text: '我的', pagePath: 'pages/my/my', icon: 'i-carbon-user' }
]
```

---

## 三、核心模块与功能详解

### 3.1 用户认证模块 (Login & Auth)

#### 功能概述
- 支持多种登录方式: 账号密码、手机号、微信、支付宝
- 双 Token 无感刷新机制
- 登录状态持久化
- 自动登录拦截

#### 核心流程

**1. 用户登录流程**
```
用户输入账号密码
    ↓
调用 tokenStore.login()
    ↓
发送 POST /miniapp/auth/loginByUsername
    ↓
后端返回 Token 信息
    ↓
tokenStore.setTokenInfo() 存储 Token
    ↓
调用 userStore.fetchUserInfo() 获取用户信息
    ↓
跳转到首页或重定向页面
```

**2. Token 刷新流程 (双Token模式)**
```
请求 API 返回 401
    ↓
http.ts 拦截器捕获
    ↓
检查是否有 refresh_token
    ↓
调用 tokenStore.refreshToken()
    ↓
发送 POST /auth/refreshToken
    ↓
获取新的 access_token
    ↓
重新发起原请求
    ↓
返回数据给调用方
```


**3. 登录拦截流程**
```
用户访问页面
    ↓
路由拦截器 (navigateToInterceptor)
    ↓
检查 tokenStore.hasLogin
    ↓
已登录 → 允许访问
    ↓
未登录 → 检查白名单
    ↓
在白名单 → 允许访问
    ↓
不在白名单 → 跳转登录页 (带 redirect 参数)
```

#### 关键代码位置
- Token 管理: `src/store/token.ts`
- 用户信息: `src/store/user.ts`
- 登录接口: `src/api/login.ts`
- 路由拦截: `src/router/interceptor.ts`
- HTTP 拦截: `src/http/interceptor.ts`

---

### 3.2 购物车模块 (Cart)

#### 功能概述
- 商品添加/删除/修改数量
- 全选/单选商品
- 实时计算总价
- 库存检查
- 滑动删除交互

#### 核心功能

**1. 购物车数据结构**
```typescript
interface CartItem {
  id: number              // 购物车项 ID
  product_id: number      // 商品 ID
  product_name: string    // 商品名称
  product_image: string   // 商品图片
  price: number           // 价格 (分)
  quantity: number        // 数量
  stock: number           // 库存
  checked: boolean        // 是否选中 (前端状态)
}
```

**2. 购物车操作流程**
```
添加商品到购物车:
  用户点击"加入购物车" 
    → 调用 addToCart(productId, quantity)
    → POST /miniapp/api/cart
    → 触发 uni.$emit('cartChanged')
    → TabBar 徽标更新

修改商品数量:
  用户点击 +/-
    → 调用 updateCartItem(cartItemId, newQuantity)
    → PUT /miniapp/api/cart/{id}
    → 更新本地状态

删除商品:
  用户滑动删除
    → 调用 deleteCartItem(cartItemId)
    → DELETE /miniapp/api/cart/{id}
    → 从列表移除

结算:
  用户点击"结算"
    → 检查选中商品
    → 检查库存
    → 跳转订单确认页 (开发中)
```

#### 关键代码位置
- 购物车页面: `src/pages/cart/cart.vue`
- 购物车接口: `src/api/cart.ts`
- 购物车工具: `src/utils/cart.ts`

---

### 3.3 收藏模块 (Favorite)

#### 功能概述
- 支持文章和商品收藏
- 收藏/取消收藏切换
- 收藏列表展示
- 收藏统计

#### 核心功能

**1. 收藏类型**
```typescript
enum FavoriteType {
  ARTICLE = 'article',  // 文章
  PRODUCT = 'product'   // 商品
}
```

**2. 收藏操作流程**
```
切换收藏状态:
  用户点击收藏按钮
    → 调用 toggleFavorite({ item_type, item_id })
    → POST /miniapp/api/favorite/toggle
    → 返回 { is_favorited: boolean }
    → 更新 UI 状态
    → 触发 uni.$emit('favoriteChanged')
    → 个人中心收藏数更新

获取收藏列表:
  进入收藏页面
    → 调用 getFavoriteList({ type, page, per_page })
    → GET /miniapp/api/favorite/list
    → 展示收藏列表

获取收藏统计:
  进入个人中心
    → 调用 getFavoriteStats()
    → GET /miniapp/api/favorite/count
    → 返回 { total, article, product }
    → 更新徽标数字
```

#### 关键代码位置
- 收藏页面: `src/pages/my/favorite.vue`
- 收藏接口: `src/api/favorite.ts`


---

### 3.4 评论系统模块 (Comment)

#### 功能概述
- 多级评论 (支持回复)
- 点赞功能
- 评论删除
- 评论统计
- 实时更新

#### 核心功能

**1. 评论数据结构**
```typescript
interface Comment {
  id: number
  article_id: number
  user_id: number
  content: string
  created_at: string
  parent_id: number | null      // 父评论 ID
  level: number                 // 评论层级
  like_count: number            // 点赞数
  reply_count: number           // 回复数
  is_liked: boolean             // 当前用户是否点赞
  user_nickname: string
  user_avatar: string
  user_role: 'author' | 'user' | 'admin'
  is_author: boolean            // 是否是作者
  reply_to_user_id: number | null
  reply_to_nickname: string | null
  children: Comment[]           // 子评论列表
}
```

**2. 评论操作流程**
```
发表评论:
  用户输入评论内容
    → 调用 CommentAPI.createComment({ article_id, content, parent_id })
    → POST /api/comments/create
    → 返回新评论数据
    → 插入到评论列表

点赞评论:
  用户点击点赞按钮
    → 调用 CommentAPI.toggleLike(commentId)
    → POST /api/comments/{id}/like
    → 返回 { is_liked, like_count }
    → 更新 UI 状态

删除评论:
  用户点击删除
    → 弹出确认框
    → 调用 CommentAPI.deleteComment(commentId)
    → DELETE /api/comments/{id}/delete
    → 从列表移除

获取评论列表:
  进入文章详情
    → 调用 CommentAPI.getArticleComments(articleId, { page, per_page })
    → GET /api/comments/article/{id}
    → 返回评论列表 (树形结构)
    → 渲染评论组件
```

#### 关键代码位置
- 评论组件: `src/components/CommentSystem.vue`
- 评论项组件: `src/components/CommentItem.vue`
- 评论接口: `src/api/comment.ts`

---

### 3.5 聊天系统模块 (Chat) ⭐核心亮点

#### 功能概述
- 实时聊天 (基于 WebSocket)
- 会话列表管理
- 在线状态显示
- 未读消息提醒
- 消息发送/接收
- 输入状态提示

#### 架构设计

**1. 三层架构**
```
┌─────────────────────────────────────┐
│  App.vue (应用层)                    │
│  - 初始化 WebSocket                  │
│  - 监听登录/登出                     │
│  - 生命周期管理                      │
└─────────────────────────────────────┘
           ↓
┌─────────────────────────────────────┐
│  socketManager (连接层)              │
│  - 单例模式                          │
│  - 连接管理                          │
│  - 事件分发                          │
│  - 心跳保活                          │
│  - 断线重连                          │
└─────────────────────────────────────┘
           ↓
┌─────────────────────────────────────┐
│  chatStore (状态层)                  │
│  - 会话列表                          │
│  - 在线状态                          │
│  - 未读数统计                        │
│  - TabBar 徽标                       │
└─────────────────────────────────────┘
           ↓
┌─────────────────────────────────────┐
│  页面层 (chat.vue / room.vue)        │
│  - 会话列表展示                      │
│  - 聊天室交互                        │
│  - 消息渲染                          │
└─────────────────────────────────────┘
```


**2. WebSocket 连接生命周期 (完整版)**

```
┌─────────────────────────────────────────────────────────────┐
│ 1. App 启动 (onLaunch)                                       │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. 检查登录状态 (tokenStore.validToken)                      │
└─────────────────────────────────────────────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ 是否已登录?            │
        └───────────────────────┘
         ↓ 是              ↓ 否
┌──────────────────┐  ┌──────────────────┐
│ 3. 初始化连接    │  │ 跳过初始化        │
│ socketManager    │  │ (等待用户登录)    │
│ .init(token)     │  └──────────────────┘
└──────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 建立 WebSocket 连接                                       │
│    - 连接到后端 Socket.IO 服务器                             │
│    - 自动加入个人房间 (NormalUser_{userId})                 │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 连接成功 (socket.on('connect'))                          │
│    - 注册全局监听器 (user_status_change, new_message)       │
│    - 启动心跳保活 (30s 间隔)                                 │
│    - 重置重连计数器                                          │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 加载会话列表 (chatStore.fetchConversations)              │
│    - HTTP 请求获取会话列表                                   │
│    - 自动加入所有会话房间 (conversation_{roomId})           │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. 开始接收实时消息                                          │
│    - 监听 new_message (新消息)                               │
│    - 监听 user_status_change (在线状态)                      │
│    - 监听 room_online_users_update (房间在线用户)            │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 8. App 切到后台 (onHide)                                     │
│    ⚠️ 策略: 保持连接，不断开                                 │
│    - 继续接收消息 (后台推送)                                 │
│    - 心跳保活继续运行                                        │
│    - 连接断开会自动重连                                      │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 9. App 回到前台 (onShow)                                     │
│    - 检查连接状态                                            │
│    - 如果断开 → 自动重连 socketManager.init(token)          │
│    - 如果连接正常 → 继续使用                                 │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 10. 用户登出 (tokenStore.logout)                            │
│     - 触发 tokenStore.$subscribe 监听器                     │
│     - 调用 socketManager.destroy()                          │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 11. 彻底清理 (destroy 方法) ⭐                               │
│     ✅ 发送 user_logout 事件通知后端                         │
│     ✅ 设置 isDestroyed = true (防止重连)                    │
│     ✅ 清除重连定时器 (reconnectTimer)                       │
│     ✅ 停止心跳定时器 (heartbeatTimer)                       │
│     ✅ 断开 Socket 连接 (socket.disconnect)                  │
│     ✅ 释放 Socket 实例 (socket = null)                      │
│     ✅ 清空会话房间缓存 (conversationRooms.clear)           │
│     ✅ 清空消息队列 (messageQueue = [])                      │
│     ✅ 清空事件监听器 (eventHandlers.clear)                  │
│     ✅ 重置全局监听器标志 (globalListenersRegistered)        │
│     ✅ 调用 chatStore.$reset() 清空聊天状态                  │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 12. 内存完全释放 ✅                                          │
│     - 所有定时器已清除                                       │
│     - 所有事件监听器已移除                                   │
│     - 所有引用已置空                                         │
│     - 防止内存泄漏                                           │
└─────────────────────────────────────────────────────────────┘
```

**关键策略说明**:

**🔹 后台保持策略**
```typescript
// App.vue - onHide
onHide(() => {
  // ⚠️ 不断开连接，保持后台接收消息
  // 原因:
  // 1. 用户可能随时切回前台
  // 2. 需要接收后台消息推送
  // 3. 小程序/APP 支持后台运行
})
```

**🔹 前台恢复策略**
```typescript
// App.vue - onShow
onShow(() => {
  const tokenStore = useTokenStore()
  if (tokenStore.validToken && !socketManager.isConnected()) {
    // 检测到连接断开，自动重连
    console.log('[App] 🔄 Reconnecting WebSocket after resume...')
    socketManager.init(tokenStore.validToken)
  }
})
```

**🔹 登出清理策略**
```typescript
// socketManager.destroy()
destroy() {
  // 1. 通知后端用户下线
  this.socket.emit('user_logout', { timestamp: new Date().toISOString() })
  
  // 2. 设置销毁标志，防止自动重连
  this.isDestroyed = true
  
  // 3. 清理所有定时器
  clearTimeout(this.reconnectTimer)
  clearInterval(this.heartbeatTimer)
  
  // 4. 断开连接并释放实例
  this.socket.disconnect()
  this.socket = null
  
  // 5. 清空所有缓存数据
  this.conversationRooms.clear()
  this.messageQueue = []
  this.eventHandlers.clear()
  
  // 6. 重置状态标志
  this.globalListenersRegistered = false
  this.status = SocketStatus.DISCONNECTED
}
```

**🔹 内存泄漏防护**
- ✅ 所有定时器都有对应的清理逻辑
- ✅ 事件监听器使用 Map 管理，统一清理
- ✅ Socket 实例置空，释放引用
- ✅ 销毁标志防止意外重连
- ✅ Store 的 $reset() 清空状态

**3. 消息流转流程**

**场景 1: 用户在聊天室内**
```
用户 A 发送消息
    ↓
调用 socketManager.sendMessage(roomId, content)
    ↓
WebSocket 发送 'send_message' 事件
    ↓
后端广播到房间内所有用户
    ↓
用户 B 的 room.vue 监听到 'new_message'
    ↓
更新消息列表
    ↓
滚动到底部
```

**场景 2: 用户不在聊天室**
```
用户 A 发送消息
    ↓
后端发送到用户 B 的个人房间
    ↓
socketManager 全局监听器捕获 'new_message'
    ↓
chatStore.updateConversationItem() 更新会话列表
    ↓
chatStore.updateTotalUnreadCount() 更新未读数
    ↓
tabbarStore.setTabbarItemBadge(2, count) 更新徽标
    ↓
用户 B 看到红点提示
```

**4. 在线状态同步**
```
用户 A 上线/下线
    ↓
后端广播 'user_status_change' 事件
    ↓
socketManager 全局监听器捕获
    ↓
chatStore.updateUserOnlineStatus(userId, isOnline)
    ↓
更新 userOnlineStatus Map
    ↓
更新会话列表中的在线状态
    ↓
UI 实时显示绿点/灰点
```

#### 核心配置

**WebSocket 配置** (`src/pages/chat/config.ts`)
```typescript
export const WEBSOCKET_CONFIG = {
  URL: 'http://localhost:5050',
  MAX_RECONNECT_ATTEMPTS: 5,
  RECONNECT_INTERVAL: 3000,
  HEARTBEAT_INTERVAL: 30000
}
```

#### 关键代码位置
- WebSocket 管理: `src/utils/socket.ts`
- 聊天状态: `src/store/chat.ts`
- 会话列表: `src/pages/chat/chat.vue`
- 聊天室: `src/pages/chat/room.vue`
- 聊天配置: `src/pages/chat/config.ts`
- 应用入口: `src/App.vue`

---

### 3.6 首页模块 (Home)

#### 功能概述
- 轮播图展示
- 分类导航
- 商品推荐
- 文章列表
- 搜索入口

#### 数据加载流程
```
进入首页
    ↓
调用 loadHomeData()
    ↓
并发请求:
  - getBanners({ placement_key: 'home_top_banner' })
  - getBanners({ placement_key: 'home_middle_banner' })
  - getHomeData()
    ↓
返回数据:
  - banners: 轮播图数据
  - categories: 分类数据
  - new_products: 新品推荐
  - featured: 精选商品
  - articles: 文章列表
  - hot_products: 热门商品
    ↓
渲染页面组件
```

#### 关键代码位置
- 首页页面: `src/pages/index/index.vue`
- 首页接口: `src/api/home.ts`
- 骨架屏: `src/pages/index/components/HomeSkeleton.vue`


---

### 3.7 个人中心模块 (My)

#### 功能概述
- 用户信息展示
- 头像/封面上传
- 订单管理入口
- 收藏统计
- 功能菜单
- 退出登录

#### 核心功能

**1. 头像上传流程**
```
用户点击头像
    ↓
调用 uni.chooseImage() 选择图片
    ↓
调用 cropImage() 裁剪图片 (1:1)
    ↓
调用 uni.uploadFile() 上传
    ↓
URL: /miniapp/user/avatar
    ↓
后端返回新头像 URL
    ↓
更新 userStore.userInfo.avatar
    ↓
UI 实时更新
```

**2. 封面上传流程**
```
用户点击"更换封面"
    ↓
调用 uni.chooseImage() 选择图片
    ↓
调用 cropImage() 裁剪图片 (16:9)
    ↓
调用 uni.uploadFile() 上传
    ↓
URL: /miniapp/user/bgCover
    ↓
后端返回新封面 URL
    ↓
更新 userStore.userInfo.bg_cover
    ↓
UI 实时更新
```

**3. 数据刷新机制**
```
页面显示 (onShow)
    ↓
调用 fetchUserData()
    ↓
防抖处理 (300ms)
    ↓
并发请求:
  - userStore.fetchUserInfo() - 用户信息
  - getFavoriteStats() - 收藏统计
  - (其他统计数据...)
    ↓
更新页面状态
```

#### 关键代码位置
- 个人中心: `src/pages/my/my.vue`
- 个人资料: `src/pages/my/profile.vue`
- 设置页面: `src/pages/my/setting.vue`
- 头像组件: `src/components/CustomPreview.vue`

---

## 四、数据/逻辑流转

### 4.1 典型操作流程图

#### 场景 1: 用户登录并浏览商品

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户打开应用                                              │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. App.vue 检查登录状态                                      │
│    - tokenStore.validToken 为空                             │
│    - 显示首页 (部分功能受限)                                 │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 用户点击"登录"                                            │
│    - 路由拦截器放行 (登录页在白名单)                         │
│    - 跳转到 /pages/login/login                              │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 用户输入账号密码                                          │
│    - 调用 tokenStore.login({ username, password })          │
│    - POST /miniapp/auth/loginByUsername                     │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 后端验证成功                                              │
│    - 返回 { access_token, refresh_token, ... }              │
│    - tokenStore.setTokenInfo() 存储 Token                   │
│    - 计算并存储过期时间                                      │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 获取用户信息                                              │
│    - 调用 userStore.fetchUserInfo()                         │
│    - GET /miniapp/user/info (自动带 Token)                  │
│    - 存储用户信息到 userStore                                │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. 初始化 WebSocket                                          │
│    - App.vue 监听到 tokenStore 变化                          │
│    - 调用 socketManager.init(token)                         │
│    - 建立 WebSocket 连接                                     │
│    - 加载聊天会话列表                                        │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 8. 跳转到首页                                                │
│    - uni.reLaunch({ url: '/' })                             │
│    - 加载首页数据 (轮播图、商品、文章)                       │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 9. 用户浏览商品                                              │
│    - 点击商品卡片                                            │
│    - 跳转到 /pages/product/detail?id=xxx                    │
│    - 加载商品详情                                            │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 10. 用户加入购物车                                           │
│     - 点击"加入购物车"按钮                                   │
│     - POST /miniapp/api/cart { product_id, quantity }       │
│     - 触发 uni.$emit('cartChanged')                         │
│     - TabBar 购物车徽标更新                                  │
└─────────────────────────────────────────────────────────────┘
```


#### 场景 2: 用户收到新消息

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 客服发送消息                                              │
│    - 客服端发送消息到后端                                    │
│    - 后端识别目标用户                                        │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. 后端推送消息                                              │
│    - 通过 WebSocket 发送 'new_message' 事件                  │
│    - 发送到用户的个人房间 (NormalUser_{userId})             │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. socketManager 接收消息                                    │
│    - 全局监听器捕获 'new_message' 事件                       │
│    - 判断用户是否在聊天室内                                  │
└─────────────────────────────────────────────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ 用户在聊天室内?        │
        └───────────────────────┘
         ↓ 是              ↓ 否
┌──────────────────┐  ┌──────────────────────────────────────┐
│ room.vue 处理    │  │ 更新会话列表                          │
│ - 添加到消息列表 │  │ - chatStore.updateConversationItem() │
│ - 滚动到底部     │  │ - 更新最新消息                        │
│ - 发送已读回执   │  │ - 未读数 +1                           │
└──────────────────┘  │ - chatStore.updateTotalUnreadCount() │
                      │ - tabbarStore.setTabbarItemBadge()   │
                      │ - TabBar 显示红点徽标                 │
                      └──────────────────────────────────────┘
                                    ↓
                      ┌──────────────────────────────────────┐
                      │ 4. 用户点击 TabBar "客服"             │
                      │    - 进入会话列表页面                 │
                      │    - 看到未读消息提示                 │
                      └──────────────────────────────────────┘
                                    ↓
                      ┌──────────────────────────────────────┐
                      │ 5. 用户点击会话                       │
                      │    - 进入聊天室 room.vue              │
                      │    - 加载历史消息                     │
                      │    - 标记为已读                       │
                      │    - 徽标消失                         │
                      └──────────────────────────────────────┘
```

#### 场景 3: Token 过期自动刷新

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户操作触发 API 请求                                     │
│    - 例如: 点击收藏按钮                                      │
│    - 调用 toggleFavorite()                                  │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. HTTP 拦截器添加 Token                                     │
│    - interceptor.ts 自动添加 Authorization 头               │
│    - 使用 tokenStore.tokenInfo.access_token                 │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 后端返回 401 Unauthorized                                 │
│    - access_token 已过期                                    │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. http.ts 响应拦截器捕获                                    │
│    - 检查 statusCode === 401                                │
│    - 检查是否有 refresh_token                               │
│    - 将原请求加入队列                                        │
└─────────────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 发起 Token 刷新                                           │
│    - 调用 tokenStore.refreshToken()                         │
│    - POST /auth/refreshToken { refreshToken }              │
└─────────────────────────────────────────────────────────────┘
                    ↓
        ┌───────────────────────┐
        │ 刷新成功?              │
        └───────────────────────┘
         ↓ 是              ↓ 否
┌──────────────────┐  ┌──────────────────────────────────────┐
│ 6. 更新 Token    │  │ 6. 刷新失败                           │
│ - 存储新 Token   │  │    - 清除登录信息                     │
│ - 重发队列请求   │  │    - 弹出登录提示                     │
│ - 返回数据       │  │    - 跳转登录页                       │
└──────────────────┘  └──────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. 用户无感知完成操作                                        │
│    - 收藏成功                                                │
│    - UI 更新                                                 │
└─────────────────────────────────────────────────────────────┘
```


### 4.2 数据流向总结

```
┌─────────────────────────────────────────────────────────────┐
│                        用户交互层                            │
│  (pages/*.vue - 页面组件)                                    │
└─────────────────────────────────────────────────────────────┘
                    ↓ 用户操作
┌─────────────────────────────────────────────────────────────┐
│                        业务逻辑层                            │
│  (store/*.ts - 状态管理)                                     │
│  - tokenStore: 认证状态                                      │
│  - userStore: 用户信息                                       │
│  - chatStore: 聊天状态                                       │
└─────────────────────────────────────────────────────────────┘
                    ↓ 调用接口
┌─────────────────────────────────────────────────────────────┐
│                        API 接口层                            │
│  (api/*.ts - 接口定义)                                       │
│  - login.ts, cart.ts, favorite.ts, comment.ts...            │
└─────────────────────────────────────────────────────────────┘
                    ↓ HTTP 请求
┌─────────────────────────────────────────────────────────────┐
│                        HTTP 封装层                           │
│  (http/http.ts + interceptor.ts)                            │
│  - 请求拦截: 添加 Token、处理参数                            │
│  - 响应拦截: 处理错误、刷新 Token                            │
└─────────────────────────────────────────────────────────────┘
                    ↓ 网络请求
┌─────────────────────────────────────────────────────────────┐
│                        后端服务                              │
│  (Flask/Django/Node.js...)                                  │
└─────────────────────────────────────────────────────────────┘
                    ↓ 返回数据
┌─────────────────────────────────────────────────────────────┐
│                        数据处理                              │
│  - 更新 Store 状态                                           │
│  - 触发 UI 更新                                              │
│  - 发送全局事件 (uni.$emit)                                  │
└─────────────────────────────────────────────────────────────┘
```

**WebSocket 数据流 (独立通道)**
```
┌─────────────────────────────────────────────────────────────┐
│                        后端 WebSocket 服务                   │
└─────────────────────────────────────────────────────────────┘
                    ↓ 推送消息
┌─────────────────────────────────────────────────────────────┐
│                        socketManager                         │
│  (utils/socket.ts - WebSocket 管理器)                        │
│  - 连接管理                                                  │
│  - 事件分发                                                  │
│  - 心跳保活                                                  │
└─────────────────────────────────────────────────────────────┘
                    ↓ 触发事件
┌─────────────────────────────────────────────────────────────┐
│                        全局监听器                            │
│  (App.vue / socketManager)                                  │
│  - 监听 new_message                                          │
│  - 监听 user_status_change                                   │
└─────────────────────────────────────────────────────────────┘
                    ↓ 更新状态
┌─────────────────────────────────────────────────────────────┐
│                        chatStore                             │
│  - 更新会话列表                                              │
│  - 更新在线状态                                              │
│  - 更新未读数                                                │
└─────────────────────────────────────────────────────────────┘
                    ↓ UI 更新
┌─────────────────────────────────────────────────────────────┐
│                        页面组件                              │
│  - chat.vue: 会话列表                                        │
│  - room.vue: 聊天室                                          │
│  - tabbar: 徽标提示                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、关键难点与亮点

### 5.1 双 Token 无感刷新机制 ⭐

**难点**:
- Token 过期时机不确定
- 多个并发请求同时触发刷新
- 刷新失败需要优雅降级

**解决方案**:
```typescript
// http.ts 核心逻辑
let refreshing = false  // 防止重复刷新
let taskQueue = []      // 请求队列

// 响应拦截器
if (statusCode === 401 && refreshToken) {
  // 1. 将失败请求加入队列
  taskQueue.push(() => resolve(http(options)))
  
  // 2. 只有第一个请求触发刷新
  if (!refreshing) {
    refreshing = true
    await tokenStore.refreshToken()
    refreshing = false
    
    // 3. 刷新成功后重发所有队列请求
    taskQueue.forEach(task => task())
    taskQueue = []
  }
}
```

**亮点**:
- 用户无感知
- 避免重复刷新
- 队列机制保证请求不丢失


---

### 5.2 WebSocket 连接管理 ⭐

**难点**:
- 连接时机控制 (登录/登出)
- 断线重连机制
- 多页面状态同步
- 时序竞争问题 (HTTP vs WebSocket)
- 后台保持与内存管理
- 登出时的彻底清理

**解决方案**:

**1. 连接生命周期绑定**
```typescript
// App.vue
tokenStore.$subscribe((mutation, state) => {
  const token = tokenStore.validToken
  
  if (token && !socketManager.isConnected()) {
    // 登录 → 初始化连接
    socketManager.init(token)
    chatStore.fetchConversations()
  } else if (!token && socketManager.isConnected()) {
    // 登出 → 销毁连接
    socketManager.destroy()
    chatStore.$reset()
  }
})
```

**2. 断线重连**
```typescript
// socketManager
private tryReconnect() {
  // 检查销毁标志，防止登出后重连
  if (this.isDestroyed) {
    console.log('[Socket] ⛔ Destroyed, skipping reconnect')
    return
  }
  
  if (this.reconnectAttempts >= this.maxReconnectAttempts) {
    uni.showToast({ title: '连接已断开，请检查网络' })
    return
  }
  
  this.reconnectAttempts++
  setTimeout(() => {
    this.connect(this.token)
  }, this.reconnectInterval)
}
```

**3. 时序竞争解决**
```typescript
// 问题: HTTP 加载会话列表 vs WebSocket 连接成功
// 解决: 缓存房间 ID，连接成功后自动补发

joinConversationRoom(roomId) {
  // 无论是否连接，都先缓存
  this.conversationRooms.add(roomId)
  
  // 如果已连接，立即加入
  if (this.isConnected()) {
    this.send('join_room', { room_id: roomId })
  }
  // 否则等待连接成功后自动加入
}

private rejoinConversationRooms() {
  // 连接成功后，自动加入所有缓存的房间
  this.conversationRooms.forEach(roomId => {
    this.send('join_room', { room_id: roomId })
  })
}
```

**4. 后台保持策略 ⭐**
```typescript
// App.vue
onHide(() => {
  // ⚠️ 策略: 不断开连接，保持后台接收消息
  // 原因:
  // 1. 用户可能随时切回前台，断开重连影响体验
  // 2. 需要接收后台消息推送 (小程序/APP 支持)
  // 3. WebSocket 有心跳保活，不会轻易断开
  // 4. 即使断开，也会自动重连
})

onShow(() => {
  // 从后台回到前台，检查连接状态
  const tokenStore = useTokenStore()
  if (tokenStore.validToken && !socketManager.isConnected()) {
    console.log('[App] 🔄 Reconnecting WebSocket after resume...')
    socketManager.init(tokenStore.validToken)
  }
})
```

**5. 彻底清理机制 ⭐**
```typescript
// socketManager.destroy()
destroy() {
  console.log('[SocketManager] 🔌 Destroying...')

  // 1. 通知后端用户下线
  if (this.socket && this.status === SocketStatus.CONNECTED) {
    try {
      this.socket.emit('user_logout', {
        timestamp: new Date().toISOString()
      })
      console.log('[SocketManager] 📤 Sent user_logout event')
    } catch (error) {
      console.warn('[SocketManager] Failed to send user_logout:', error)
    }
  }

  // 2. 设置销毁标志，防止重连
  this.isDestroyed = true

  // 3. 清除所有定时器
  if (this.reconnectTimer) {
    clearTimeout(this.reconnectTimer)
    this.reconnectTimer = null
  }
  this.stopHeartbeat()  // 清除心跳定时器

  // 4. 断开连接并释放实例
  if (this.socket) {
    this.socket.disconnect()
    this.socket = null  // ⚠️ 释放引用，防止内存泄漏
  }

  // 5. 清空所有缓存数据
  this.status = SocketStatus.DISCONNECTED
  this.reconnectAttempts = 0
  this.conversationRooms.clear()  // 清空房间缓存
  this.messageQueue = []          // 清空消息队列

  // 6. 清除所有事件监听器
  this.eventHandlers.clear()      // ⚠️ 防止内存泄漏
  this.globalListenersRegistered = false

  console.log('[SocketManager] ✅ Destroyed')
}
```

**内存泄漏防护清单**:
- ✅ **定时器清理**: reconnectTimer、heartbeatTimer 都有对应的 clear
- ✅ **事件监听器清理**: eventHandlers Map 统一清空
- ✅ **Socket 实例释放**: socket = null 释放引用
- ✅ **缓存数据清空**: conversationRooms、messageQueue 清空
- ✅ **防止意外重连**: isDestroyed 标志阻止 tryReconnect
- ✅ **Store 状态清理**: chatStore.$reset() 清空聊天状态

**亮点**:
- 单例模式，全局唯一连接
- 自动重连，用户无感知
- 心跳保活，防止连接断开
- 事件驱动，解耦页面逻辑
- 后台保持，不影响消息接收
- 彻底清理，无内存泄漏风险

---

### 5.3 路由拦截与登录鉴权 ⭐

**难点**:
- 多种跳转方式 (navigateTo, switchTab, reLaunch...)
- 白名单/黑名单策略
- 登录后重定向
- TabBar 页面特殊处理

**解决方案**:

**1. 统一拦截器**
```typescript
// router/interceptor.ts
export const navigateToInterceptor = {
  invoke({ url, query }) {
    const tokenStore = useTokenStore()
    const path = parseUrlToObj(url).path
    
    // 已登录 → 放行
    if (tokenStore.hasLogin) {
      return true
    }
    
    // 白名单 → 放行
    if (judgeIsExcludePath(path)) {
      return true
    }
    
    // 其他 → 跳转登录页 (带 redirect)
    const redirectUrl = `${LOGIN_PAGE}?redirect=${encodeURIComponent(url)}`
    uni.navigateTo({ url: redirectUrl })
    return false
  }
}

// 注册拦截器
uni.addInterceptor('navigateTo', navigateToInterceptor)
uni.addInterceptor('switchTab', navigateToInterceptor)
uni.addInterceptor('reLaunch', navigateToInterceptor)
uni.addInterceptor('redirectTo', navigateToInterceptor)
```

**2. 登录后重定向**
```typescript
// login.vue
const redirect = route.query.redirect || HOME_PAGE

await tokenStore.login(loginForm)

if (isPageTabbar(redirect)) {
  uni.switchTab({ url: redirect })
} else {
  uni.navigateTo({ url: redirect })
}
```

**亮点**:
- 统一拦截，无遗漏
- 支持重定向，用户体验好
- 白名单灵活配置

---

### 5.4 自定义 TabBar 实现 ⭐

**难点**:
- 多种策略支持 (原生/自定义/有缓存/无缓存)
- 徽标动态更新
- 鼓包按钮实现
- 页面缓存控制

**解决方案**:

**1. 策略模式**
```typescript
// tabbar/config.ts
export const TABBAR_STRATEGY_MAP = {
  NO_TABBAR: 0,
  NATIVE_TABBAR: 1,
  CUSTOM_TABBAR_WITH_CACHE: 2,
  CUSTOM_TABBAR_WITHOUT_CACHE: 3
}

export const selectedTabbarStrategy = TABBAR_STRATEGY_MAP.CUSTOM_TABBAR_WITH_CACHE
```

**2. 徽标管理**
```typescript
// tabbar/store.ts
export const tabbarStore = {
  setTabbarItemBadge(index, count) {
    const item = customTabbarList[index]
    if (count > 0) {
      item.badge = count > 99 ? '99+' : count
    } else {
      item.badge = undefined
    }
  }
}
```

**3. 鼓包按钮**
```vue
<!-- tabbar/index.vue -->
<view v-if="item.isBulge" class="bulge-item">
  <image class="bulge-icon" :src="bulgeImg" />
</view>
```

**亮点**:
- 灵活配置，支持多种策略
- 徽标实时更新
- 支持鼓包按钮
- 图标支持多种方式 (UnoCSS/iconfont/image)


---

### 5.5 状态持久化 ⭐

**难点**:
- 小程序环境限制
- 数据序列化/反序列化
- 敏感信息安全

**解决方案**:

```typescript
// store/index.ts
import { createPersistedState } from 'pinia-plugin-persistedstate'

const store = createPinia()
store.use(
  createPersistedState({
    storage: {
      getItem: uni.getStorageSync,
      setItem: uni.setStorageSync
    }
  })
)

// 使用
export const useTokenStore = defineStore('token', () => {
  // ...
}, {
  persist: true  // 开启持久化
})
```

**亮点**:
- 自动持久化，无需手动存储
- 刷新页面状态不丢失
- 适配 uni-app 存储 API

---

### 5.6 全局事件通信 ⭐

**难点**:
- 跨页面/组件通信
- 事件监听管理
- 内存泄漏风险

**解决方案**:

```typescript
// 发送事件
uni.$emit('cartChanged')
uni.$emit('favoriteChanged')

// 监听事件
onMounted(() => {
  uni.$on('cartChanged', handleCartChange)
})

// 清理事件
onUnmounted(() => {
  uni.$off('cartChanged', handleCartChange)
})
```

**应用场景**:
- 购物车数量更新 → TabBar 徽标
- 收藏状态变化 → 个人中心统计
- 消息接收 → 会话列表更新

**亮点**:
- 解耦组件通信
- 实时响应变化
- 简单易用

---

### 5.7 骨架屏优化 ⭐

**难点**:
- 首屏加载体验
- 骨架屏与真实内容匹配
- 加载状态管理

**解决方案**:

```vue
<!-- pages/index/index.vue -->
<template>
  <HomeSkeleton v-if="isLoading" />
  <view v-else class="home-page">
    <!-- 真实内容 -->
  </view>
</template>

<script setup>
const isLoading = ref(true)

onMounted(async () => {
  await loadHomeData()
  isLoading.value = false
})
</script>
```

**亮点**:
- 提升用户体验
- 减少白屏时间
- 骨架屏组件化复用

---

## 六、开发规范与最佳实践

### 6.1 代码规范

**1. 命名规范**
- 组件: PascalCase (CommentSystem.vue)
- 文件: kebab-case (user-info.ts)
- 变量/函数: camelCase (getUserInfo)
- 常量: UPPER_SNAKE_CASE (API_BASE_URL)
- Store: useXxxStore (useTokenStore)

**2. 目录规范**
- 页面组件放在 `pages/` 下
- 全局组件放在 `components/` 下
- 页面专属组件放在 `pages/xxx/components/` 下
- API 接口按模块拆分文件

**3. TypeScript 规范**
- 所有接口定义类型
- 避免使用 `any`
- 善用泛型和类型推导
- 导出类型定义供外部使用

**4. 样式规范**
- 优先使用 UnoCSS 原子类
- 复杂样式使用 SCSS
- 避免全局样式污染
- 使用 scoped 限定作用域

### 6.2 Git 提交规范

使用 Commitlint + Husky 强制规范:

```bash
feat: 添加新功能
fix: 修复 bug
docs: 更新文档
style: 代码样式更改
refactor: 代码重构
perf: 性能优化
test: 增加测试
build: 改变构建工具
revert: 撤销上一次 commit
chore: 更新构建流程或工具
```

### 6.3 开发流程

**1. 启动开发**
```bash
# 安装依赖
pnpm install

# H5 开发
pnpm dev

# 微信小程序开发
pnpm dev:mp

# APP 开发
pnpm dev:app
```

**2. 代码检查**
```bash
# ESLint 检查
pnpm lint

# 自动修复
pnpm lint:fix

# TypeScript 类型检查
pnpm type-check
```

**3. 构建发布**
```bash
# H5 构建
pnpm build:h5

# 微信小程序构建
pnpm build:mp

# APP 构建
pnpm build:app
```


### 6.4 性能优化建议

**1. 图片优化**
- 使用 WebP 格式
- 图片懒加载
- 压缩图片大小
- CDN 加速

**2. 代码优化**
- 路由懒加载
- 组件按需引入
- 分包加载 (pages-sub)
- Tree Shaking

**3. 请求优化**
- 接口缓存
- 防抖/节流
- 并发请求控制
- 骨架屏占位

**4. 渲染优化**
- 虚拟列表 (长列表)
- 条件渲染 (v-if vs v-show)
- 计算属性缓存
- 避免深层嵌套

---

## 七、环境配置

### 7.1 环境变量

项目支持多环境配置，环境变量文件位于 `env/` 目录:

```
env/
├── .env.development    # 开发环境
├── .env.test          # 测试环境
└── .env.production    # 生产环境
```

**主要环境变量**:
```bash
# 应用配置
VITE_APP_TITLE=unibest
VITE_APP_PORT=9000
VITE_APP_PUBLIC_BASE=/

# 后端接口
VITE_SERVER_BASEURL=http://localhost:5000

# 代理配置 (仅 H5)
VITE_APP_PROXY_ENABLE=true
VITE_APP_PROXY_PREFIX=/api

# 小程序配置
VITE_UNI_APPID=your-app-id
VITE_WX_APPID=your-wx-appid

# 其他配置
VITE_DELETE_CONSOLE=false
VITE_SHOW_SOURCEMAP=false
```

### 7.2 构建配置

**Vite 配置** (`vite.config.ts`)
- 插件配置: UniPages, UniLayouts, UnoCSS, AutoImport
- 路径别名: `@` → `src/`
- 代理配置: H5 开发环境代理
- 构建优化: 代码分割、压缩、Tree Shaking

**TypeScript 配置** (`tsconfig.json`)
- 严格模式
- 路径映射
- 类型声明文件

**UnoCSS 配置** (`uno.config.ts`)
- 预设: @unocss/preset-uni
- 自定义规则
- 安全列表 (safelist)

---

## 八、部署指南

### 8.1 H5 部署

**1. 构建**
```bash
pnpm build:h5
```

**2. 输出目录**
```
dist/build/h5/
```

**3. Nginx 配置示例**
```nginx
server {
  listen 80;
  server_name your-domain.com;
  
  root /path/to/dist/build/h5;
  index index.html;
  
  location / {
    try_files $uri $uri/ /index.html;
  }
  
  # API 代理
  location /api {
    proxy_pass http://backend-server:5000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
```

### 8.2 微信小程序部署

**1. 构建**
```bash
pnpm build:mp
```

**2. 输出目录**
```
dist/build/mp-weixin/
```

**3. 上传发布**
- 使用微信开发者工具打开 `dist/build/mp-weixin`
- 点击"上传"按钮
- 在微信公众平台提交审核

### 8.3 APP 部署

**1. 构建**
```bash
pnpm build:app
```

**2. 云打包**
- 使用 HBuilderX 打开 `dist/build/app`
- 选择"发行" → "原生 App-云打包"
- 配置证书和签名
- 提交打包

**3. 本地打包**
- 配置 Android Studio / Xcode
- 使用 HBuilderX 离线打包
- 生成 APK / IPA 文件

---

## 九、常见问题 (FAQ)

### 9.1 开发问题

**Q: 如何切换后端接口地址?**
A: 修改 `env/.env.development` 中的 `VITE_SERVER_BASEURL`

**Q: 如何添加新的 API 接口?**
A: 
1. 在 `src/api/` 下创建或编辑对应模块文件
2. 定义接口函数和类型
3. 在页面/组件中导入使用

**Q: 如何添加新页面?**
A: 
1. 在 `src/pages/` 下创建 `.vue` 文件
2. 使用 `definePage()` 定义页面配置
3. 约定式路由会自动生成 `pages.json`

**Q: Token 刷新失败怎么办?**
A: 
- 检查 `refresh_token` 是否过期
- 检查后端刷新接口是否正常
- 查看 `src/http/http.ts` 中的刷新逻辑

**Q: WebSocket 连接失败怎么办?**
A:
- 检查后端 WebSocket 服务是否启动
- 确认 `src/pages/chat/config.ts` 中的 URL 配置正确
- 查看浏览器控制台的连接错误信息
- 检查 Token 是否有效

**Q: App 切到后台后 WebSocket 会断开吗?**
A: 
- **不会主动断开**，策略是保持连接
- 原因: 需要接收后台消息推送
- 如果网络原因断开，回到前台会自动重连
- 心跳保活机制会维持连接

**Q: 用户登出后 WebSocket 会彻底清理吗?**
A: 
- **会彻底清理**，调用 `socketManager.destroy()`
- 清理内容包括:
  - ✅ 发送 user_logout 事件通知后端
  - ✅ 断开 Socket 连接
  - ✅ 清除所有定时器 (重连、心跳)
  - ✅ 清空事件监听器
  - ✅ 清空缓存数据 (房间、消息队列)
  - ✅ 设置销毁标志防止重连
  - ✅ 释放 Socket 实例引用
- 不会有内存泄漏风险

**Q: 如何调试 WebSocket 连接?**
A:
- 查看控制台日志 (带 [Socket] 前缀)
- 使用浏览器开发者工具的 Network → WS 标签
- 检查 `socketManager.getStatus()` 返回的状态
- 在 `src/utils/socket.ts` 中添加更多日志

### 9.2 部署问题

**Q: H5 部署后接口 404?**
A: 
- 检查 Nginx 代理配置
- 确认 `VITE_SERVER_BASEURL` 配置正确
- 检查跨域配置

**Q: 小程序审核被拒?**
A: 
- 检查隐私政策
- 确认功能描述准确
- 移除未使用的权限申请

**Q: APP 打包失败?**
A: 
- 检查证书配置
- 确认 AppID 正确
- 查看 HBuilderX 控制台错误信息


---

## 十、技术亮点总结

### 10.1 架构设计亮点

1. **分层架构清晰**
   - 页面层 → 状态层 → 接口层 → HTTP 层
   - 职责明确，易于维护

2. **模块化设计**
   - 按业务模块拆分目录
   - 组件化开发，高复用性

3. **类型安全**
   - 全面的 TypeScript 类型定义
   - 接口类型化，减少运行时错误

4. **状态管理规范**
   - Pinia 统一管理全局状态
   - 支持持久化，刷新不丢失

### 10.2 功能实现亮点

1. **双 Token 无感刷新**
   - 用户无感知的 Token 刷新
   - 请求队列机制，避免重复刷新

2. **WebSocket 实时通信**
   - 单例模式管理连接
   - 自动重连，心跳保活
   - 全局事件监听，状态同步

3. **路由拦截鉴权**
   - 统一拦截所有跳转方式
   - 支持白名单/黑名单策略
   - 登录后自动重定向

4. **自定义 TabBar**
   - 支持多种策略切换
   - 徽标动态更新
   - 支持鼓包按钮

5. **评论系统**
   - 多级评论支持
   - 点赞功能
   - 实时更新

6. **收藏功能**
   - 支持多种类型收藏
   - 统计数据实时更新
   - 全局事件通知

### 10.3 用户体验亮点

1. **骨架屏加载**
   - 减少白屏时间
   - 提升首屏体验

2. **防抖节流**
   - 避免重复请求
   - 优化性能

3. **错误处理**
   - 统一错误提示
   - 友好的错误信息

4. **加载状态**
   - 全局 Loading
   - 局部 Loading
   - 骨架屏占位

5. **交互反馈**
   - 点击反馈
   - 滑动删除
   - 下拉刷新

### 10.4 工程化亮点

1. **代码规范**
   - ESLint + Prettier
   - Husky + Commitlint
   - Git Hooks 自动检查

2. **环境管理**
   - 多环境配置
   - 环境变量隔离

3. **构建优化**
   - Vite 快速构建
   - 代码分割
   - Tree Shaking

4. **开发体验**
   - 热更新
   - 类型提示
   - 自动导入

---

## 十一、未来优化方向

### 11.1 功能扩展

- [ ] 订单管理系统
- [ ] 支付功能集成
- [ ] 优惠券系统
- [ ] 积分商城
- [ ] 会员等级体系
- [ ] 分享功能
- [ ] 消息推送

### 11.2 性能优化

- [ ] 虚拟列表优化长列表
- [ ] 图片懒加载
- [ ] 接口缓存策略
- [ ] CDN 加速
- [ ] 预加载优化

### 11.3 体验优化

- [ ] 暗黑模式
- [ ] 国际化 (i18n)
- [ ] 无障碍支持
- [ ] 手势操作优化
- [ ] 动画效果增强

### 11.4 技术升级

- [ ] 升级到 Vue 3.5+
- [ ] 引入 Pinia Colada (数据获取)
- [ ] 使用 VueUse 工具库
- [ ] 引入单元测试
- [ ] 引入 E2E 测试

---

## 十二、参考资料

### 12.1 官方文档

- [uni-app 官方文档](https://uniapp.dcloud.net.cn/)
- [Vue 3 官方文档](https://cn.vuejs.org/)
- [Pinia 官方文档](https://pinia.vuejs.org/zh/)
- [Vite 官方文档](https://cn.vitejs.dev/)
- [UnoCSS 官方文档](https://unocss.dev/)
- [TypeScript 官方文档](https://www.typescriptlang.org/zh/)

### 12.2 项目相关

- [unibest 官方文档](https://unibest.tech/)
- [unibest GitHub](https://github.com/feige996/unibest)
- [sard-uniapp 组件库](https://sutpc.github.io/sard-uniapp/)

### 12.3 技术文章

- WebSocket 实时通信最佳实践
- uni-app 性能优化指南
- TypeScript 类型体操
- Pinia 状态管理实战

---

## 附录

### A. 核心文件清单

**配置文件**
- `package.json` - 项目依赖
- `vite.config.ts` - Vite 配置
- `tsconfig.json` - TypeScript 配置
- `uno.config.ts` - UnoCSS 配置
- `manifest.config.ts` - 应用清单
- `pages.config.ts` - 页面配置

**核心代码**
- `src/main.ts` - 应用入口
- `src/App.vue` - 应用根组件
- `src/store/token.ts` - 认证状态管理
- `src/store/user.ts` - 用户信息管理
- `src/store/chat.ts` - 聊天状态管理
- `src/http/http.ts` - HTTP 请求封装
- `src/http/interceptor.ts` - 请求拦截器
- `src/utils/socket.ts` - WebSocket 管理器
- `src/router/interceptor.ts` - 路由拦截器
- `src/tabbar/config.ts` - TabBar 配置

### B. API 接口清单

**认证相关**
- `POST /miniapp/auth/loginByUsername` - 账号密码登录
- `POST /miniapp/auth/loginByPhone` - 手机号登录
- `POST /miniapp/auth/wxLogin` - 微信登录
- `POST /miniapp/auth/register` - 用户注册
- `POST /auth/refreshToken` - 刷新 Token
- `GET /miniapp/user/info` - 获取用户信息
- `POST /miniapp/user/updateInfo` - 更新用户信息
- `GET /miniapp/auth/logout` - 退出登录

**购物车相关**
- `GET /miniapp/api/cart` - 获取购物车列表
- `POST /miniapp/api/cart` - 添加商品到购物车
- `PUT /miniapp/api/cart/{id}` - 更新购物车商品
- `DELETE /miniapp/api/cart/{id}` - 删除购物车商品
- `DELETE /miniapp/api/cart/clear` - 清空购物车

**收藏相关**
- `POST /miniapp/api/favorite/toggle` - 切换收藏状态
- `POST /miniapp/api/favorite/check` - 检查收藏状态
- `GET /miniapp/api/favorite/list` - 获取收藏列表
- `GET /miniapp/api/favorite/count` - 获取收藏统计
- `POST /miniapp/api/favorite/remove` - 批量取消收藏
- `POST /miniapp/api/favorite/clear` - 清空收藏

**评论相关**
- `GET /api/comments/article/{id}` - 获取文章评论
- `POST /api/comments/create` - 发表评论
- `POST /api/comments/{id}/like` - 点赞评论
- `DELETE /api/comments/{id}/delete` - 删除评论
- `GET /api/comments/statistics/{id}` - 获取评论统计

**聊天相关**
- `GET /miniapp/api/chat/rooms` - 获取会话列表
- WebSocket 事件:
  - `send_message` - 发送消息
  - `new_message` - 接收消息
  - `user_status_change` - 用户状态变化
  - `join_room` - 加入房间
  - `leave_room` - 离开房间

---

## 结语

本文档详细介绍了项目的架构设计、功能实现、数据流转和技术亮点。通过阅读本文档，你应该能够:

1. ✅ 快速了解项目的整体架构
2. ✅ 理解各个模块的职责和功能
3. ✅ 掌握核心业务逻辑的流转过程
4. ✅ 学习项目中的技术亮点和最佳实践
5. ✅ 能够独立进行功能开发和维护

如有疑问或建议，欢迎提出 Issue 或 PR。

---

**文档版本**: v1.0.0  
**更新日期**: 2025-01-19  
**作者**: Kiro AI Assistant  
**项目**: unibest 电商小程序

